pandas>=2.0
numpy>=1.24
openpyxl>=3.1
scikit-learn>=1.3
lightgbm>=4.0        # optional; falls back to sklearn HistGradientBoosting if absent
pulp>=2.7            # ILP solver (CBC bundled)
rapidfuzz>=3.0       # fuzzy player-name matching
requests>=2.31       # web enrichment of foreign newcomers
reportlab>=4.0       # standings PDF
